spring:
  application:
    name: gold-trading-dashboard-backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/gold_trading_bot}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123456}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        default_schema: dasboard
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    default-schema: dasboard
    ignore-migration-patterns: "*:missing"
server:
  port: ${APP_PORT:8088}

app:
  jwt:
    secret: ${APP_JWT_SECRET_BASE64:Q2hhbmdlVGhpc1NlY3JldEtleUZvclByb2R1Y3Rpb24xMjM0NTY3ODkwQQ}
    access-token-minutes: ${APP_JWT_ACCESS_TOKEN_MINUTES:15}
    refresh-token-days: ${APP_JWT_REFRESH_TOKEN_DAYS:30}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://172.188.217.183:5173}
  mt5:
    encryption-key: ${APP_MT5_ENCRYPTION_KEY:change-this-key-16}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true


